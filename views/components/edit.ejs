<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secret Feature Editor</title>
</head>
<body>
    <h1>Edit/Update components!</h1>
    <section>
        <form action="/components/<% component._id %>?_method=put" method="post">
            <label for="name">Name:</label>
            <input type="text" name="name" value="<%= component.name %>">
            <br>
            <label for="cost">Cost:</label>
            <input type="number" name="cost" value="<%= component.cost %>">
            <br>
            <label for="">Description:</label>
            <input type="text" name="description" value="<%= component.description %>">
            <br>
            <label for="tipo">Tipo:</label>
            <select name="tipo" id="">
                <% for (let enojado of tipito) { %>
                    <option value="<%= enojado %>" <% component.tipo === enojado ? 'selected' : '' %>><%= enojado %></option>
                <% } %>
            </select>
            <br>
            <label for="fabricante">Fabricante:</label>
            <select name="fabricante" id="">
                <% for (let fabi of fabricantesito) { %>
                    <option value="<%= fabi %>"><%= fabi %></option>
                <% } %>
            </select>
            <button>Editar</button>
        </form>
    </section>
</body>
</html>